# AI Agents

Desktop AI agents for GitHub and Google Workspace.

## Apps

- `github-agent` — GitHub issues, PRs, repos, and notifications
- `google-agent` — Gmail, Calendar, Drive, Docs, and Sheets

Each app is a Tauri desktop client with a Next.js UI and a Python backend.

Copy `backend/.env.example` to `backend/.env` before running.
